function showFact(type) {
    // This could be expanded to show more detailed information
    console.log(`Showing fact about ${type}`);
    
    // Example of displaying a modal or expanding the fact card
    alert(`You clicked on the ${type} muscle fact!`);
}